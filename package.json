{
  "name": "encantados",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Encantados — A Trilha das Oito Medalhas",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "checar": "tsc --noEmit",
    "testar": "node --test \"src/**/*.test.ts\"",
    "test": "node --test \"src/**/*.test.ts\"",
    "arte": "node tools/render.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "@types/three": "^0.169.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
